{
  "name": "test-native",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mocha dev src/App.qml.ts",
    "build": "tsc && mocha build src/App.qml.ts",
    "type-gen": "mocha type-gen ./src",
    "doctor": "mocha doctor",
    "postinstall": "cd node_modules/@mocha/native && npx napi build --platform --release 2>/dev/null || true"
  },
  "dependencies": {
    "@mocha/core": "workspace:*",
    "@mocha/native": "workspace:*",
    "@mocha/qml": "workspace:*",
    "@mocha/shared": "workspace:*",
    "@mocha/tokens": "workspace:*"
  },
  "devDependencies": {
    "@mocha/cli": "workspace:*",
    "@mocha/kit": "workspace:*",
    "typescript": "^5.5.0",
    "tsx": "^4.15.0"
  }
}
